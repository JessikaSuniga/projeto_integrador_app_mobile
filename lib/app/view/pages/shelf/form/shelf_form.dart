import 'package:flutter/material.dart';

class ShelfForm extends StatelessWidget {
  const ShelfForm({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("Estante");
  }
}
